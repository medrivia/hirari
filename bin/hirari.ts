#!/usr/bin/env bun
if (typeof Bun === "undefined" || !process.versions.bun) {
  throw new Error("This app needs to be executed with Bun.");
}

// id:closing
console.log("Thanks for using Hirari!");
// #tag: message
